import React from 'react';
import Avatar from '../../../components/Avatar';
import PostImage from '../../../components/PostImage';

const Notification = ({ data }) => {
  // get avatar data with hook
  // get post data with hook
  return (
    <div>
      <Avatar />
      <div></div>
      <PostImage />
    </div>
  );
};

export default Notification;
