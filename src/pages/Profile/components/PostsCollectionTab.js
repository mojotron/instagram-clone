import React from 'react';

const PostsCollectionTab = ({ accountType, targetData }) => {
  const { error, isPending, document } = targetData.response;
  // collect posts
  // display posts

  return <div>PostsCollectionTab</div>;
};

export default PostsCollectionTab;
